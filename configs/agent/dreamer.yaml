# @package agent
defaults:
  - default

_target_: agent.dreamer.DreamerAgent
name: dreamer
cfg: ???
obs_space: ???
act_spec: ???
reward_norm: {momentum: 0.95, scale: 1.0, eps: 1e-8}
reward_fn: "task"
actor_ent: 0

train_target_reach: True

world_model:
  encoder: {mlp_keys: 'proprio', cnn_keys: '$^', norm: layer, cnn_depth: 48, cnn_kernels: [4, 4, 4, 4], mlp_layers: [400, 400, 400, 400], coordConv: False} # act: elu
  decoder: {mlp_keys: 'proprio', cnn_keys: '$^', norm: layer, cnn_depth: 48, cnn_kernels: [5, 5, 6, 6], mlp_layers: [400, 400,  400, 400]} # act: elu
  grad_heads: [decoder]
  replay.capacity: 1e6
  device: cuda
  name: dreamer