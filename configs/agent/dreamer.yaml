# @package agent
defaults:
  - default

_target_: agent.dreamer.DreamerAgent
name: dreamer
cfg: ???
obs_space: ???
act_spec: ???
reward_norm: {momentum: 1.0, scale: 1.0, eps: 1e-8}
reward_fn: "task"
actor_ent: 0

train_target_reach: True

world_model:
  encoder: {mlp_keys: 'proprio', cnn_keys: 'rgb', norm: none, cnn_depth: 48, cnn_kernels: [4, 4, 4, 4], mlp_layers: 4, mlp_units: 400, coordConv: False, symlog_inputs: True, output_dist: False} # act: elu
  decoder: {mlp_keys: 'proprio', cnn_keys: 'rgb', norm: none, cnn_depth: 48, cnn_kernels: [5, 5, 6, 6], mlp_layers: 4, mlp_units: 400, symlog_inputs: True, output_dist: False} # act: elu
  grad_heads: [decoder]
  replay.capacity: 1e6
  device: cuda
  name: dreamer